# Система тем Habimori

## Обзор

Приложение поддерживает светлую и тёмную темы с плавными переходами между ними.

## Архитектура

### ThemeProvider
Расположение: `src/components/ThemeProvider.tsx`

- Управляет текущей темой через React Context
- Сохраняет выбор пользователя в `localStorage` (ключ: `habimori-theme`)
- Автоматически определяет системную тему при первом посещении
- Добавляет/удаляет класс `dark` на `<html>` элементе

### CSS переменные
Расположение: `app/globals.css`

Все цвета определены через CSS переменные для обеих тем:

```css
/* Светлая тема (по умолчанию) */
:root {
    --background: #f8fafc;
    --foreground: #0f172a;
    --surface: #ffffff;
    --surface-elevated: #ffffff;
    --border: #e2e8f0;
    --border-light: #f1f5f9;
    --text-primary: #0f172a;
    --text-secondary: #475569;
    --text-muted: #64748b;
    --text-faint: #94a3b8;
    --accent: #0f172a;
    --accent-hover: #1e293b;
}

/* Тёмная тема */
:root.dark {
    --background: #0c1222;
    --foreground: #f1f5f9;
    --surface: #1a2332;
    --surface-elevated: #243044;
    --border: #334155;
    --border-light: #1e293b;
    --text-primary: #f1f5f9;
    --text-secondary: #cbd5e1;
    --text-muted: #94a3b8;
    --text-faint: #64748b;
    --accent: #e2e8f0;
    --accent-hover: #f1f5f9;
}
```

## Использование в компонентах

### Tailwind классы
Используйте цвета тем через Tailwind:

```tsx
// Фоны
className="bg-background"     // Основной фон страницы
className="bg-surface"        // Фон карточек/блоков
className="bg-surface-elevated" // Приподнятые элементы

// Текст
className="text-text-primary"   // Основной текст
className="text-text-secondary" // Второстепенный текст
className="text-text-muted"     // Приглушённый текст
className="text-text-faint"     // Очень бледный текст

// Границы
className="border-border"       // Основные границы
className="border-border-light" // Лёгкие границы

// Акценты (кнопки)
className="bg-accent text-surface hover:bg-accent-hover"
```

### Статусные цвета
Статусные цвета остаются семантическими и адаптируются через opacity:

```tsx
// Успех (зелёный)
className="bg-emerald-500/10 text-emerald-600 border-emerald-500/30"

// Ошибка (красный)
className="bg-rose-500/10 text-rose-600 border-rose-500/30"

// Предупреждение/в процессе (оранжевый)
className="bg-amber-400"
```

## Переключатель темы

Компонент: `src/components/ThemeSwitcher.tsx`

Расположен в header рядом с кнопкой выхода. Показывает:
- 🌙 Иконку луны в светлой теме (переключить на тёмную)
- ☀️ Иконку солнца в тёмной теме (переключить на светлую)

### Использование хука useTheme

```tsx
import { useTheme } from "@/src/components/ThemeProvider";

function MyComponent() {
  const { theme, toggleTheme, setTheme } = useTheme();
  
  return (
    <button onClick={toggleTheme}>
      Текущая тема: {theme}
    </button>
  );
}
```

## Дизайн-решения

### Почему не чисто чёрный фон?
Тёмная тема использует тёмно-синий оттенок (`#0c1222`) вместо чисто чёрного (`#000000`):
- Более приятен для глаз при длительном использовании
- Лучше контраст с текстом и акцентами
- Современный и профессиональный вид

### Transition
Все элементы с `transition-colors` для плавного переключения тем.

### Opacity для статусов
Вместо жёстких цветов (`bg-rose-50`) используются цвета с opacity (`bg-rose-500/10`):
- Работают одинаково в обеих темах
- Автоматически адаптируются к фону

## Добавление новых компонентов

При создании новых компонентов:

1. Используйте CSS переменные темы через Tailwind
2. Избегайте hardcoded цветов (`bg-white`, `text-slate-900`)
3. Добавляйте `transition-colors` для плавных переходов
4. Тестируйте в обеих темах

## Локализация

Названия тем локализованы:
- `theme.dark` - "Тёмная тема" / "Dark theme"
- `theme.light` - "Светлая тема" / "Light theme"
